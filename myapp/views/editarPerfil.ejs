<!DOCTYPE html>
<html lang="en">


<%- include('./partials/head') %>


    <body>

        <% if(locals.user !=undefined){ %>
            <%- include('./partials/headerLogeado') %>

                <% } else {%>
                    <%- include('./partials/headerDeslogeado') %>

                        <% } %>

                            <main>
                                <div class="uk-section uk-section-small container">
                                    <div class="padi">
                                        <h2>Editar datos de perfil</h2>
                                    </div>
                                    <form enctype="multipart/form-data" action="/usuario/ejecutaredit/<%= usuario.id%> " method="POST" class="pi">

                                        <h4> Nombre de usuario </h4><input type="search" name="nombre"
                                            value="<%= usuario.nombre_usuario %> " class="inputformulario"
                                            onfocus="search()" placeholder="Ej. Leomessi10">
                                        <h4> Nuevo email </h4><input type="email" name="email"
                                            value="<%= usuario.email %>" class="inputformulario" onfocus="search()"
                                            placeholder="Email">
                                        <h4> Nueva contraseña </h4><input type="password" name="password" value=""
                                            class="inputformulario" onfocus="search()" placeholder="Contrasena">
                                        <h4>Agregar nueva foto de perfil</h4><input
                                            class="form-input" type="file" name="fotoperfilnueva"
                                            class="inputformulario" onfocus="search()" placeholder="Foto de perfil"
                                            accept="image/png">
                                        <button type="submit"> Agregar </button>
                                        <br>
                                        <br>
                                        <button type="submit"> Cambiar </button>
                                        <br>
                                        <br>
                                        <br>
                                        <br>




                                    </form>
                                </div>
                            </main>



                            <%- include('./partials/footer') %>



    </body>

</html>